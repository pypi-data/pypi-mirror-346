Reviewed the code for `get_group_info_ex`. Removed unused logging import. Nested models `GroupOwnerId`, `GroupBindGuildIds`, `GroupExtFlameData`, `GroupExcludeGuildIds` inside `ExtInfo`, `ExtInfo` inside `Data`, and `Data` inside `GetGroupInfoExRes` as per the nested class standard. Updated the `status` field definition in `GetGroupInfoExRes` to include the default value and correct description.