{
  "properties": {
    "endpoints": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "inputs",
          "outputs",
          "url"
        ]
      }
    }
  },
  "required": [
    "name",
    "desc",
    "version",
    "endpoints"
  ]
}