[project]
name = "calculator-mcp"
version = "0.1.4"
description = "MCP server that gives you calculator tools"
authors = [
    {name = "Azam Afridi",email = "azamafridi.ai@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "calculator_mcp", from = "src"}]

[tool.poetry.scripts]
calculator-mcp = "calculator_mcp.server:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
