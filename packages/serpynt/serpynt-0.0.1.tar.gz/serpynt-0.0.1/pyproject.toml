[project]
name = "serpynt"
version = "0.0.1"
description = "A minimal Python static site generator (SSG)."
authors = [
  { name="Tyler W. Phass", email="codezealot+dev@pm.me" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
