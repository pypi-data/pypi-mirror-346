"""
Discogs MCP Server main entry point.

This module serves as the main entry point for running the Discogs MCP server
when the package is executed directly (e.g., python -m mcp_server_discogs).
"""

from mcp_server_discogs import main

# Execute the main function when this module is run directly
main()
