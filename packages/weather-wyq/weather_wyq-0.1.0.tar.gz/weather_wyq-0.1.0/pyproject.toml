[project]
name = "weather-wyq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
[tool.uv]
main-module = "weather"  # 指定 main.py 为主入口

