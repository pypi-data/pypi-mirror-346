[project]
name = "openpecha"
version = "2.1.8"
authors = [
    {name = "Tashi Tsering", email = "tashitsering@esukhia.org"},
    {name = "Tenzin Tsundue", email = "tsundue@esukhia.org"}
]
description = "OpenPecha toolkit version 2"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

# Keep all regular dependencies
dependencies = [
  "pydantic >= 2.7.4",
  "stam ==  0.9.0",
  "diff-match-patch==20230430",
  "PyGithub >= 2.3.0",
  "pyewts == 0.2.0",
  "toml >= 0.10.2",
  "GitPython>=3.1.0, <4.0",
  "botok >= 0.8.12",
  "subprocess.run == 0.0.8",
  "python-docx >= 1.1.2",
  "fast_antx == 0.0.1",
  "bo_sent_tokenizer == 0.0.1",
  "pecha_org_tools == 0.0.8",
  "google-auth-httplib2 >= 0.2.0",
  "google-api-python-client >= 2.156.0",
  "docx2python == 3.3.0",
  "boto3 >= 1.34.0",
  "botocore >= 1.34.0",
  "rdflib>=5.0.0",
  "fonttools[unicode] >= 4.37.3",
  "beautifulsoup4 >= 4.12.0"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "pre-commit",
]

[project.urls]
"Homepage" = "https://github.com/OpenPecha/openpecha-project-template"
"Bug Tracker" = "https://github.com/OpenPecha/openpecha-project-template/issues"

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
branch = "main"
upload_to_pypi = true
upload_to_release = true # This is for creating GitHub releases
build_command = "pip install build && python -m build"

# Add these for commit analysis and version bumping logic:
commit_message_style = "angular" # Uses angular conventions for parsing
major_on_breaking_change = true  # Default, but good to be explicit
minor_on_feat = true             # Default for angular style
patch_on_fix = true              # Default for angular style

# Optional but good for consistency:
tag_format = "v{version}" # Default, explicitly stating 's version tag tag_format