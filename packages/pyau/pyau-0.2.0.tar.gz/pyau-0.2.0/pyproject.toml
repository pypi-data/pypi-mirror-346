[project]
name = "pyau"
version = "0.2.0"
description = "A Python library mimicking Luau."
readme = "README.md"
license = {text = "MIT"}
authors = [{ name = "nahhkandy" }]
dependencies = []
requires-python = ">=3.7"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"