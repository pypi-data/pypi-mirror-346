[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "endura-sdk"
version = "0.1.2"
description = "SDK for Edge AI Device Agent"
authors = [
    { name="EnduraAI", email="hi@michaelkirschbaum.com" }
]
dependencies = [
    "torch>=2.0",
    "psutil",
    "numpy"
]
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["."]
include = ["sdk"]