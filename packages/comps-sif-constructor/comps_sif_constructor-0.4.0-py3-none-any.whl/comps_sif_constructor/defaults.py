# Apptainer definition file (local)
DEFINITION_FILE = 'apptainer.def'
# ID with the COMPS SIF asset (local)
SIF_ID_FILE = 'sif.id'
# Name of the SIF file (comps)
SIF_FILE = 'comps-sif-constructor.sif'
# Name of the experiment (comps)
WORK_ITEM_NAME = "Singularity Build"
# Requirements file (comps + local)
REQUIREMENTS = None
# Output file (local)
OUTPUT_FILE = 'data_brick.json'
# Experiment ID file (local)
EXPERIMENT_ID_FILE = 'experiment.id'