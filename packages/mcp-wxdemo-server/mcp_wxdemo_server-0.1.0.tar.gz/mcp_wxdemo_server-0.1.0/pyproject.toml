[project]
name = "mcp-wxdemo-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "WangXiao", email = "wb-wx869082@alibaba-inc.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.7.1",
]

[project.scripts]
mcp-wxdemo-server = "mcp_wxdemo_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
