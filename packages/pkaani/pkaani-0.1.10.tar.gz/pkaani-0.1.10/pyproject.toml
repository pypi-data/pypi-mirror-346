# pyproject.toml
[build-system]
# XXX: If your project needs other packages to build properly, add them to this list.
requires = ["numpy", "scipy", "torch", "torchani", "scikit-learn==1.6.1", "ase", "joblib", "setuptools"]
build-backend = "setuptools.build_meta"
