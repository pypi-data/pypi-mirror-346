[project]
name = "syft-event"
version = "0.2.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "syft-rpc==0.2.0",
    "pathspec>=0.12.1",
    "pydantic>=2.10.4",
    "watchdog>=6.0.0",
    "loguru>=0.7.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
syft-rpc = { workspace = true }
