[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "av-video"
version = "0.0.1"
description = "Convert videos to image frames and vice versa."
readme = "README.md"
license = "MIT"
authors = [{ name = "Yi Zhang", email = "yizhang.dev@gmail.com" }]
requires-python = ">=3.10"
dependencies = ["av", "pillow", "tqdm"]

[project.urls]
Repository = "https://github.com/imyizhang/av-video"

[project.optional-dependencies]
dev = ["ruff"]