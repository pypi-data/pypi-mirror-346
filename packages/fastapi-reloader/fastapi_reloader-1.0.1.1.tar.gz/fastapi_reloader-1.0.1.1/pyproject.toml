[project]
name = "fastapi-reloader"
version = "1.0.1.1"
readme = "README.md"
dependencies = [
    "fastapi~=0.115",
    "hmr>=0.4,<0.6",
]

[project.urls]
Homepage = "https://github.com/promplate/hmr"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
