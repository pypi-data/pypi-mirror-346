[project]
name = "mcp-pokemon-server-cstest"
version = "4.5.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[project.scripts]
mcp-pokemon-server-cstest = "mcp_pokemon_server_cstest:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
