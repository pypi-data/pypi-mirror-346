from setuptools import setup

# This setup.py is provided for backwards compatibility with older tooling,
# but the project's metadata and build configuration is primarily defined in pyproject.toml.
setup()