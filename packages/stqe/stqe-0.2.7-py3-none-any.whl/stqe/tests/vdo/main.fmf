description: There are tests for VDO (Virtual Data Optimizer) product in RHEL.
requires_install:
 - vdo
 - kmod-kvdo
tester: Filip Suba <fsuba@redhat.com>
component:
  - vdo
  - kmod-kvdo

# only raid has really enough memory to satisfy some large memory requirements
# iscsi_2T and raid are the only ones with guaranteed big storage under
tags: [crypt, fcoe, iscsi_10G, iscsi_2T, local, multipath, raid]
