[project]
name = "crawlio-py"
version = "0.1.0"
description = "Python SDK for the Crawlio web scraping API"
authors = [{ name="Kishan Sharma", email="kisshan07@outlook.com" }]
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.7"
dependencies = [
    "requests"
]