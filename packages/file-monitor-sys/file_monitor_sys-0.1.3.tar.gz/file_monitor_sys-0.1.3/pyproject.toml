[project]
name = "file-monitor-sys"
version = "0.1.3"
description = "A Python library for monitoring file access and integrity."
authors = [{name = "Your Name", email = "your.email@example.com"}]
readme = "README.md"
license = "MIT"
requires-python = ">=3.6"
dependencies = [
    "watchdog",
    "yagmail"
]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
