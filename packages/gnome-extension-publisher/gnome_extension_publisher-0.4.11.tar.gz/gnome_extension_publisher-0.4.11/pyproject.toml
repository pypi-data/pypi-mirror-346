[tool.poetry]
name = "gnome-extension-publisher"
version = "0.4.11"
description = ""
readme = "README.md"
repository = "https://github.com/dmzoneill/gnome-extension-publisher"
authors = ["David O Neill <dmz.oneill@gmail.com>"]


[tool.poetry.dependencies]
python = "^3.8"
requests = "*"
typer = "*"

[tool.poetry.dev-dependencies]
pytest = "^6.0.2"

[tool.poetry.scripts]
gep = "gnome_extension_publisher.cli:app"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"


