[project]
name = "excelmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "scratchxyz", email = "monologist@mail.ustc.edu.cn" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.3.0",
    "pywin32>=310",
]

[project.scripts]
excelmcp = "excelmcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
