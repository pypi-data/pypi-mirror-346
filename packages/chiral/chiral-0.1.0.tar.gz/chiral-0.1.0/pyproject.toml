[project]
name = "chiral"
version = "0.1.0"
description = "Python utilities I use across various projects."
readme = "README.md"
authors = [
    { name = "Hart Traveller", email = "ruling_fiction.0b@icloud.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
