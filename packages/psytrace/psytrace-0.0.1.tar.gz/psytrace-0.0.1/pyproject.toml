[project]
name = "psytrace"
version = "0.0.1"
description = "A tool for psychological traces"
authors = [{name = "Michael J. Lee", email = "mil@mit.edu"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"