[project]
name = "weather-mcp-kmy"
version = "0.1.0"
description = "通过位置获取某一个城市的天气"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.7.1",
]
[project.scripts]
weather-mcp-kmy="main:main"