# pyproject.toml

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "StatOmics"
version = "0.6"
description = "description"
authors = [
    {name = "Nathan Li"}
]
urls = {"Homepage" = "https://github.com/nathanxli/StatOmics"}
dependencies = [
    "rpy2",
    "pathlib"
]

[tool.setuptools]
include-package-data = true