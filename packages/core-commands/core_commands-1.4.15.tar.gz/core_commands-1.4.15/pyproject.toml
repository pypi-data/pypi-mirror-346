[tool.poetry]
name = "core-commands"
version = "1.4.15"
description = ""
authors = ["Ricardo Torrellas <ricardo.torrejas@gmail.com>"]
readme = "README.md"
packages = [
{include = "core_commands"},
{include = "core_commands/**/*.py"}
]

[tool.poetry.dependencies]
python = "^3.11"
core-scripts = "^0.4.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
