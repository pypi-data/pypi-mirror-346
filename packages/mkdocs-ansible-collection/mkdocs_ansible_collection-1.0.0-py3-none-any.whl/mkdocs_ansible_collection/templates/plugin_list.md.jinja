# {{ plugin_type }} plugins

This is the list of `{{ plugin_type }}` plugins in the `{{ fqcn }}` collection:

| Plugin | Description |
| ------ | ------ |
{% for plugin, plugin_data in plugins.items() %}
| [{{ plugin }}](./{{ plugin | replace(fqcn + '.', '') }}.md) | {% if 'doc' in plugin_data and 'description' in plugin_data['doc'] %}{{ plugin_data['doc']['description'] | join(' ') }}{% else %}-{% endif %}|
{% endfor %}
