doc: |
  A workflow used to test we can correctly initialise the `Orientations` class from
  YAML parametrisations.

tasks:
  - schema: define_orientations
    inputs:
      orientations:
        data:
          - [0, 0, 0]
          - [0, 45, 0]
        unit_cell_alignment:
          x: a
        representation:
          type: euler
          euler_definition: bunge
          euler_is_degrees: true
