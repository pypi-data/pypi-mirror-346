[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyCREA"  
version = "0.0.7"        
description = "A Python package to parse and manage CREA word vectors"
authors = [
    { name = "Alex Skitowski", email = "askitowski@mcw.edu" }
]
dependencies = [
    "numpy",
    "scikit-learn",
    "scipy",
]