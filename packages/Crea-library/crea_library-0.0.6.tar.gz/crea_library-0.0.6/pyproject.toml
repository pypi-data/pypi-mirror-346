[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Crea-library"  
version = "0.0.6"        
description = "A package to parse CREA word vectors"
authors = [
    { name = "Alex Skitowski", email = "alexskit@gmail.com" }
]
dependencies = [
    "numpy",
    "scikit-learn",
    "scipy",
]