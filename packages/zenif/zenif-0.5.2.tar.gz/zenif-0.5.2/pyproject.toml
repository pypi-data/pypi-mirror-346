[build-system]
requires      = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zenif"
version = "0.5.2"
description = "Various utilties to enhance your projects"
readme = "README.md"
authors = [{ name = "dombom" }]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = ["utils", "logger", "log"]
dependencies = [
    "black",
    "pygments",
    "colorama",
]
requires-python = ">=3.12"

[project.urls]
GitHub = "https://github.com/DomBom16/zenif"