[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "andromeda-api"
version = "0.1.7"
description = "Andromeda API Inference Client by Prosilico AB"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    { name="Morgan Ekmefjord", email="morgan@prosilico.com" }
]
dependencies = [
    "requests",
    "python-dotenv"
]

[project.urls]
Homepage = "https://www.prosilico.com"
