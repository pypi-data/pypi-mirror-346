[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "s-aes-util"
version = "0.2.0"
description = "A simple implementation of the Simplified AES (S-AES) encryption algorithm"
readme = "README.md"
authors = [
  { name = "A Guy", email = "myemailisprivate@gmail.com" }
]
dependencies = []

[project.scripts]
s-aes = "s_aes.cli:main"
